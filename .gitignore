# =================================================================
# ZEQIUM - MASTER GITIGNORE (versión FINAL y completa - marzo 2026)
# =================================================================

# --- 1. SEGURIDAD Y SECRETOS (CRÍTICO - NUNCA SUBIR) ---
**/src/config/*-identity.json
**/config/*-identity.json
issuer-identity.json
hotel-identity.json
police-identity.json
*.identity.json
.env
.env.*
.env.local
.env.development
.env.production
token.txt
credentials.json
*.pem
*.key
*.crt
*.cer
*.csr
*.p12
**/crypto-config/                  # ¡Imprescindible! Todas las claves Fabric (MSP, TLS) están aquí
**/channel-artifacts/              # Bloques, tx, genesis — generados
**/wallet/                         # Wallets locales de Fabric

# --- 2. BLOCKCHAIN / FABRIC (GENERADOS Y LOCALES) ---
**/*.block
**/*.tx
genesis.block
*.tar.gz                           # Paquetes chaincode (zeqium.tar.gz)
vendor/                            # Go dependencies vendidas
go.sum                             # Varía por máquina, regenera con go mod tidy
**/bin/                            # cryptogen, configtxgen, etc.
**/builders/
**/fabric-samples/
**/build/

# --- 3. BACKEND & SHARED (NODE.JS) ---
**/node_modules/
**/package-lock.json               # Genera ruido y conflictos; regenera con npm i
**/npm-debug.log*
**/yarn-error.log
dist/
build/
.next/
coverage/

# --- 4. MOBILE / FLUTTER ---
**/mobile/**/.dart_tool/
**/mobile/**/.idea/
**/mobile/**/build/
**/mobile/**/GeneratedPluginRegistrant.*
**/mobile/**/Generated.xcconfig
**/mobile/**/.flutter-plugins
**/mobile/**/.flutter-plugins-dependencies
**/mobile/**/ios/Flutter/flutter_export_environment.sh
**/mobile/**/ios/Runner/GeneratedPluginRegistrant.*

# --- 5. DOCKER Y ENTORNO LOCAL ---
docker-compose.override.yml
*.log
*.tmp
*.swp
*.swo
.DS_Store
Thumbs.db
.vscode/
.idea/
*.code-workspace

# --- 6. SCRIPTS Y PRUEBAS LOCALES ---
generateKey.js
generateHotelKey.js
test-checkin.js
test-*.js
**/tests/local/

# --- 7. ARCHIVOS QUE APARECEN EN TU GIT STATUS Y NO DEBEN SUBIRSE ---
../docker-compose.yaml             # El antiguo con hardcodes (ya borrado)
../package-lock.json               # Raíz del repo (probablemente generado)
../blockchain/network/zeqium.tar.gz # Paquete chaincode generado
../blockchain/chaincode/chaincode  # Posible duplicado o carpeta temporal
../blockchain/chaincode/zeqium     # Si es la fuente del chaincode, mejor no versionarla entera (usa go.mod y go.sum)
../blockchain/network/crypto-config/ # ¡Imprescindible! Claves Fabric
../blockchain/network/connection-*.json # Si tienen rutas absolutas locales (/home/mgf00042/...), mejor regenerarlos o usar variables